/** Orbital Editorial access gateway: a formal Command-governed entry instrument for POLARIS workspaces. */
import { ArrowLeft, ArrowRight, Check, LockKeyhole, Orbit, ShieldCheck, Telescope, UserRound } from "lucide-react";
import { useState } from "react";
import { Link, useLocation } from "wouter";
import { NorthStarMark } from "@/components/NorthStarMark";
import { type PortalRole, usePolaris } from "@/contexts/PolarisContext";

type Notice = { tone: "neutral" | "success" | "pending" | "error"; text: string } | null;
const accessRoles: Array<{ id: PortalRole; label: string; mission: string; icon: typeof Telescope; target: string; profileEmail: string }> = [
  { id: "Explorer", label: "Explorer", mission: "Trace verified research, expeditions, media and learning records.", icon: Telescope, target: "/user", profileEmail: "aarav.kulkarni@mail.com" },
  { id: "Researcher", label: "Researcher", mission: "Contribute evidence and coordinate the field-to-publication path.", icon: Orbit, target: "/researcher", profileEmail: "rmeeranair1@polaris.in" },
  { id: "Command", label: "Command", mission: "Approve access, steward evidence, and oversee observatory operations.", icon: ShieldCheck, target: "/admin", profileEmail: "command@polaris.in" },
];

export default function AccessGateway() {
  const [, navigate] = useLocation();
  const { signIn, requestAccess } = usePolaris();
  const [role, setRole] = useState<PortalRole>("Explorer");
  const [mode, setMode] = useState<"sign-in" | "request">("sign-in");
  const [name, setName] = useState("");
  const [email, setEmail] = useState("");
  const [notice, setNotice] = useState<Notice>(null);
  const activeRole = accessRoles.find((item) => item.id === role) ?? accessRoles[0];
  const Icon = activeRole.icon;
  const loadProfile = () => { setEmail(activeRole.profileEmail); setName(role === "Researcher" ? "Dr. Meera Nair" : role === "Explorer" ? "Aarav Kulkarni" : "POLARIS Command"); setMode("sign-in"); setNotice({ tone: "neutral", text: "Approved access profile loaded. Verify the access grant to continue." }); };
  const verify = () => { const result = signIn(email, role); if (result.state === "approved") { navigate(activeRole.target); return; } setNotice({ tone: result.state === "pending" ? "pending" : "error", text: result.message }); };
  const transmit = () => { if (!email.trim()) { setNotice({ tone: "error", text: "Enter an institutional email address before transmitting an access request." }); return; } const result = requestAccess(name, email, role as Exclude<PortalRole, "Command">); setNotice({ tone: result.state === "approved" ? "success" : "pending", text: result.message }); if (result.state === "approved") setMode("sign-in"); };
  const selectRole = (next: PortalRole) => { setRole(next); setMode(next === "Command" ? "sign-in" : mode); setNotice(null); };
  return <main className="access-page"><div className="access-scanline" aria-hidden="true" /><header className="access-header"><Link href="/" className="access-brand"><NorthStarMark /><span>POLARIS</span></Link><div><span>SPACE TECHNOLOGY × POLAR SCIENCE</span><b>ACCESS CONTROL</b></div></header><section className="access-layout"><aside className="access-brief"><p className="eyebrow"><LockKeyhole size={14} /> Admin-governed observatory</p><h1>Access the<br /><em>polar</em> network.</h1><p>POLARIS connects public discovery, research contribution and scientific command through a single verified access ledger.</p><div className="access-protocol"><span>01 <b>Request</b><small>Identify the role you need.</small></span><span>02 <b>Review</b><small>Command verifies the access case.</small></span><span>03 <b>Observe</b><small>Enter your authorised workspace.</small></span></div><div className="access-earth" aria-hidden="true"><i /><i /><span>POLAR ORBIT / 716 KM</span></div></aside><section className="access-console" aria-labelledby="access-title"><div className="access-console-top"><div><p>POLARIS / ACCESS CONTROL</p><h2 id="access-title">Identity<br />gateway.</h2></div><NorthStarMark /></div><div className="access-role-grid">{accessRoles.map((item) => { const RoleIcon = item.icon; return <button type="button" onClick={() => selectRole(item.id)} className={role === item.id ? "access-role access-role--active" : "access-role"} key={item.id}><RoleIcon size={18} /><span>{item.label}</span><i /></button>; })}</div><div className="access-role-readout"><Icon size={19} /><div><span>REQUESTED WORKSPACE</span><b>{activeRole.label}</b></div><p>{activeRole.mission}</p></div>{role !== "Command" && <div className="access-mode"><button type="button" onClick={() => { setMode("sign-in"); setNotice(null); }} className={mode === "sign-in" ? "access-mode--active" : ""}>Sign in</button><button type="button" onClick={() => { setMode("request"); setNotice(null); }} className={mode === "request" ? "access-mode--active" : ""}>Request access</button></div>}<div className="access-form">{mode === "request" && role !== "Command" && <label><span>FULL NAME</span><input value={name} onChange={(event) => setName(event.target.value)} placeholder="Your name" /></label>}<label><span>INSTITUTIONAL EMAIL</span><input value={email} onChange={(event) => setEmail(event.target.value)} onKeyDown={(event) => event.key === "Enter" && (mode === "request" ? transmit() : verify())} placeholder="name@institution.in" /></label>{notice && <div className={`access-notice access-notice--${notice.tone}`}>{notice.tone === "success" && <Check size={15} />}{notice.tone === "pending" && <Orbit size={15} />}{notice.tone === "error" && <LockKeyhole size={15} />}<span>{notice.text}</span></div>}<button type="button" className="access-submit" onClick={mode === "request" && role !== "Command" ? transmit : verify}>{mode === "request" && role !== "Command" ? <>Transmit access request <ArrowRight size={17} /></> : <>Verify access grant <ArrowRight size={17} /></>}</button></div><div className="access-demo"><div><UserRound size={14} /><span>APPROVED ACCESS PROFILE</span></div><button type="button" onClick={loadProfile}>Load {activeRole.label.toLowerCase()} profile <ArrowRight size={14} /></button></div><p className="access-footnote"><ShieldCheck size={13} /> Explorer and Researcher access is enabled only after Command approval. Permissions remain active for the current session.</p></section></section><footer className="access-footer"><Link href="/"><ArrowLeft size={15} /> Return to mission overview</Link><span>POLARIS / ACCESS LEDGER / 2026</span></footer></main>;
}
